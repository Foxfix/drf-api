node {
    stage('SCM Checkout') {
        git credentialsId: 'django-server', url: 'https://github.com/Foxfix/drf-api'
    }
    stage('Build image') {
        def myImage = docker.build("my-image:latest")

        myImage.inside {
            sh 'docker ps -a'
        }
    }
}